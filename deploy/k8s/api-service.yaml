apiVersion: v1
kind: Service
metadata:
  name: sil-api
  labels:
    app: sil
    tier: api
spec:
  selector:
    app: sil
    tier: api
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
